<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.hs.emirim.sookhee.redonorpets">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity" />
        <activity android:name=".JoinActivity2" />
        <activity android:name=".JoinActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".EditPasswdActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".DonationActivity" />
        <activity android:name=".ShelterProfileActivity" />
        <activity android:name=".StoryDetailActivity" />
        <activity
            android:name="kr.hs.emirim.sookhee.redonorpets.ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>